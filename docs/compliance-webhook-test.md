# Temporary — compliance webhook delivery test (round 2)

Throwaway PR to confirm the PLX MC Compliance GitHub App now delivers
`pull_request` events to `/api/compliance/webhook`. **Do not merge — will be closed.**
